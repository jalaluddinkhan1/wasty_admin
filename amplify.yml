version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - node -v
        - npm ci --ignore-scripts
    build:
      commands:
        - env | grep -e WASTY_ -e NEXT_PUBLIC_ -e FIREBASE_ADMIN_ | sed 's/=.*$/=***/' || true
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
